// Firestore Security Rules for FR8X-CON — Full Permissions & Trial Module Ready
// Grants full read and write permissions to all authenticated users across all collections.

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ── Helper Functions ──
    function isAuthenticated() {
      return request.auth != null;
    }

    // ── Global Match Rule for Full Authenticated Access ──
    match /{document=**} {
      allow read, write: if isAuthenticated();
    }

    // ── Explicit Collection Rules for Explicit Clarity ──

    // Users
    match /users/{userId} {
      allow read, write: if isAuthenticated();
    }

    // Profiles
    match /profiles/{profileId} {
      allow read, write: if isAuthenticated();
    }

    // Companies
    match /companies/{companyId} {
      allow read, write: if isAuthenticated();
    }

    // Posts / Feed (Allows liking, reposting, commenting by any user)
    match /posts/{postId} {
      allow read, write: if isAuthenticated();
    }

    // Comments
    match /comments/{commentId} {
      allow read, write: if isAuthenticated();
    }

    // Likes
    match /likes/{likeId} {
      allow read, write: if isAuthenticated();
    }

    // Bookmarks
    match /bookmarks/{bookmarkId} {
      allow read, write: if isAuthenticated();
    }

    // Auctions & Items
    match /auctions/{auctionId} {
      allow read, write: if isAuthenticated();
    }

    match /auctionItems/{itemId} {
      allow read, write: if isAuthenticated();
    }

    match /auctionParticipants/{participantId} {
      allow read, write: if isAuthenticated();
    }

    // Bids (Allows placing & updating bids)
    match /bids/{bidId} {
      allow read, write: if isAuthenticated();
    }

    // Live Ranks
    match /liveRanks/{rankId} {
      allow read, write: if isAuthenticated();
    }

    // Rates
    match /rates/{rateId} {
      allow read, write: if isAuthenticated();
    }

    // Awards & Blacklists
    match /awards/{awardId} {
      allow read, write: if isAuthenticated();
    }

    match /blacklists/{blacklistId} {
      allow read, write: if isAuthenticated();
    }

    // Currencies
    match /currencies/{currencyId} {
      allow read, write: if isAuthenticated();
    }

    // Notifications
    match /notifications/{notificationId} {
      allow read, write: if isAuthenticated();
    }

    // Audit & Logs
    match /audit/{logId} {
      allow read, write: if isAuthenticated();
    }

    match /logs/{logId} {
      allow read, write: if isAuthenticated();
    }

    // Settings
    match /settings/{settingId} {
      allow read, write: if isAuthenticated();
    }

    // Locations & Audits
    match /locations/{locationId} {
      allow read, write: if isAuthenticated();
    }

    match /location_audit/{auditId} {
      allow read, write: if isAuthenticated();
    }

    // Jobs & Applications
    match /jobs/{jobId} {
      allow read, write: if isAuthenticated();
    }

    match /jobApplications/{appId} {
      allow read, write: if isAuthenticated();
    }

    // Transactions
    match /transactions/{txId} {
      allow read, write: if isAuthenticated();
    }

    // Connections
    match /connections/{connectionId} {
      allow read, write: if isAuthenticated();
    }

    // Backups, Ads & Push Tokens
    match /backups/{backupId} {
      allow read, write: if isAuthenticated();
    }

    match /ads/{adId} {
      allow read, write: if isAuthenticated();
    }

    match /push_tokens/{tokenId} {
      allow read, write: if isAuthenticated();
    }

    match /tags/{tagId} {
      allow read, write: if isAuthenticated();
    }

    // Invoices, Receipts & Stamps
    match /invoices/{invoiceId} {
      allow read, write: if isAuthenticated();
    }

    match /receipts/{receiptId} {
      allow read, write: if isAuthenticated();
    }

    match /stamps/{stampId} {
      allow read, write: if isAuthenticated();
    }

    match /otps/{otpId} {
      allow read, write: if isAuthenticated();
    }
  }
}
